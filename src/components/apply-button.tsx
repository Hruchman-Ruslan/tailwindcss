export interface ApplyButtonProps {}

export default function ApplyButton({}: ApplyButtonProps) {
  return (
    <section className='p-10'>
      <button className='btn-primary'>Save changes</button>
    </section>
  );
}
